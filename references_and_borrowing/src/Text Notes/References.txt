=>references allows to reger a value of a variable without taking its ownership.
+created by using & operator.

=>Immutable references
+multiple immutable references  are allowed. only immutable are allowed

=>Mutable references
+ &mut syntac is used only for mutable references.


